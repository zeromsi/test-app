spring:
  application:
    name: test-app
    message: Welcome To test-app
server:
  port: 8080

management:
  server:
    port: 8081
  endpoints:
    web:
      exposure:
        include: "*"
    health.show-details: "always"
